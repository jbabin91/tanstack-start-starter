---
description: 'Comprehensive patterns and best practices for TanStack Start projects'
globs: '*'
---

# TanStack Start Patterns

A comprehensive guide to building scalable applications with TanStack Start.

## 🚀 Quick Navigation

### Core Patterns

- 🏗️ [Core](../core/architecture.mdc) - Project setup, architecture, and conventions
- 🔄 [Workflow](../workflow.mdc) - Development process and guidelines

### Application Layers

- 🎨 [Frontend](../frontend.mdc) - UI components, state management, and routing
- 💾 [Data](../data.mdc) - Data modeling, queries, and mutations
- 🔌 [API](../api.mdc) - Server functions and API patterns

### Quality & Security

- 🔒 [Security](../security.mdc) - Authentication, authorization, and protection
- ✅ [Testing](../testing.mdc) - Testing strategies and patterns
- 📊 [Quality](../quality.mdc) - Code quality and standards
- 🚀 [Deployment](../deployment.mdc) - Deployment and release process

## 🔍 Pattern Relationships

```mermaid
graph TD
    A[Frontend] --> B[Data]
    A --> C[API]
    B --> C
    D[Security] --> A
    D --> B
    D --> C
    E[Testing] --> A
    E --> B
    E --> C
    F[Quality] --> A
    F --> B
    F --> C
    G[Deployment] --> A
    G[Deployment] --> B
    G[Deployment] --> C
```

## 🌳 Decision Trees

### Technology Selection

```mermaid
graph TD
    A[New Feature] --> B{UI Component?}
    B -->|Yes| C{Complex State?}
    B -->|No| D{Data Operation?}
    C -->|Yes| E[Zustand]
    C -->|No| F[Local State]
    D -->|Yes| G{Write Operation?}
    D -->|No| H[TanStack Query]
    G -->|Yes| I[TanStack Mutation]
    G -->|No| J[TanStack Query]
```

### Implementation Strategy

```mermaid
graph TD
    A[Implementation] --> B{Performance Critical?}
    B -->|Yes| C[Optimize First]
    B -->|No| D[Implement First]
    C --> E{Memory or Speed?}
    D --> F[Add Tests]
    E -->|Memory| G[Reduce Bundle]
    E -->|Speed| H[Add Caching]
```

## 📚 Quick Reference

### Common Solutions

#### State Management

```typescript
// Global state with Zustand
const useStore = create<AppState>((set) => ({
  count: 0,
  increment: () => set((state) => ({ count: state.count + 1 })),
}));

// Server state with TanStack Query
const query = useQuery({
  queryKey: ['todos'],
  queryFn: fetchTodos,
});
```

#### Data Fetching

```typescript
// Basic query
const users = useQuery({
  queryKey: ['users'],
  queryFn: fetchUsers,
});

// Mutation with optimistic update
const mutation = useMutation({
  mutationFn: updateUser,
  onMutate: async (newUser) => {
    await queryClient.cancelQueries({ queryKey: ['users'] });
    const previousUsers = queryClient.getQueryData(['users']);
    queryClient.setQueryData(['users'], (old) => [...old, newUser]);
    return { previousUsers };
  },
});
```

#### Routing

```typescript
// Type-safe routing
const Route = createFileRoute('/users/$userId')({
  loader: ({ params }) => fetchUser(params.userId),
  component: UserProfile,
});
```

## 🎯 Best Practices

1. **Type Safety**

   - Use TypeScript strict mode
   - Leverage type inference
   - Define explicit types for APIs

2. **Performance**

   - Implement proper caching
   - Use code splitting
   - Optimize bundle size

3. **Security**

   - Validate all inputs
   - Implement proper auth
   - Follow security headers

4. **Testing**
   - Write unit tests
   - Add integration tests
   - Include E2E tests

## ⚠️ Anti-Patterns

1. **State Management**

   - Avoid prop drilling
   - Don't mix global and server state
   - Don't duplicate state

2. **Data Fetching**

   - Avoid nested queries
   - Don't fetch in loops
   - Don't ignore errors

3. **Performance**
   - Don't skip memoization
   - Avoid unnecessary re-renders
   - Don't ignore bundle size

## 🔍 Search Patterns

- [By Use Case](../index.mdc#use-cases)
- [By Technology](../index.mdc#technologies)
- [By Problem Domain](../index.mdc#domains)

## 📈 Version History

- v1.0.0 - Initial release
- v1.1.0 - Added decision trees
- v1.2.0 - Enhanced pattern relationships

## 🔄 Updates

Last updated: 2024-02-18
Next review: 2024-03-18

## 📖 Contributing

See [Workflow](../workflow.mdc) for contribution guidelines and process.
