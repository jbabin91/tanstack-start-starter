---
description: 'Security headers patterns and best practices for HTTP security'
globs: '**/security/**/*.{ts,tsx}'
---

# Security Headers Patterns

## Overview

Our security headers implementation provides comprehensive HTTP security:

- Content Security Policy
- XSS Protection
- CSRF Protection
- Frame Options
- Referrer Policy

## Core Technologies

- Helmet.js
- CSP Builder
- CORS configuration
- Custom middleware
- Security monitoring

## Implementation

### 1. Security Middleware

```typescript
// security/middleware.ts
export const securityMiddleware = [
  // Basic security headers
  helmet(),

  // Content Security Policy
  helmet.contentSecurityPolicy({
    directives: {
      defaultSrc: ["'self'"],
      scriptSrc: ["'self'", "'unsafe-inline'"],
      styleSrc: ["'self'", "'unsafe-inline'"],
      imgSrc: ["'self'", 'data:', 'https:'],
      connectSrc: ["'self'", 'https://api.example.com'],
      fontSrc: ["'self'", 'https://fonts.gstatic.com'],
      objectSrc: ["'none'"],
      mediaSrc: ["'self'"],
      frameSrc: ["'none'"],
    },
  }),

  // CORS configuration
  cors({
    origin: process.env.ALLOWED_ORIGINS?.split(','),
    methods: ['GET', 'POST', 'PUT', 'DELETE', 'OPTIONS'],
    allowedHeaders: ['Content-Type', 'Authorization'],
    credentials: true,
  }),
];
```

### 2. CSRF Protection

```typescript
// security/csrf.ts
export const csrfProtection = [
  // Generate CSRF token
  csrf({
    cookie: {
      httpOnly: true,
      secure: process.env.NODE_ENV === 'production',
      sameSite: 'strict',
    },
  }),

  // Validate CSRF token
  (req: Request, res: Response, next: NextFunction) => {
    if (req.method === 'GET') {
      return next();
    }

    if (!req.csrfToken()) {
      return next(new SecurityError('Invalid CSRF token'));
    }

    next();
  },
];
```

### 3. Security Headers Configuration

```typescript
// security/headers.ts
export const securityHeaders = {
  'Strict-Transport-Security': 'max-age=31536000; includeSubDomains',
  'X-Frame-Options': 'DENY',
  'X-Content-Type-Options': 'nosniff',
  'X-XSS-Protection': '1; mode=block',
  'Referrer-Policy': 'strict-origin-when-cross-origin',
  'Permissions-Policy': 'camera=(), microphone=(), geolocation=()',
};
```

## Best Practices

### 1. Header Configuration

- Use secure defaults
- Regular updates
- Environment-specific settings
- Proper testing
- Monitoring

### 2. CSP Implementation

- Strict policies
- Nonce generation
- Report-only mode
- Violation reporting
- Regular updates

### 3. CORS Security

- Specific origins
- Required methods
- Allowed headers
- Credentials handling
- Preflight requests

### 4. Monitoring

- Header validation
- Policy violations
- Attack detection
- Regular audits
- Incident response

## Related Patterns

- [Authentication](./authentication-patterns.mdc) for auth implementation
- [Authorization](./authorization-patterns.mdc) for access control
- [Data Protection](./data-protection.mdc) for data security
- [Error Handling](../core/error-handling.mdc) for security errors
- [API Security](../api/security-patterns.mdc) for API protection
