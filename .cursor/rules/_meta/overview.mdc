---
description: 'Core project concepts, structure, and development workflow'
category: 'meta'
related: ['index', 'glossary', 'decisions']
status: 'stable'
lastUpdated: '2024-02-18'
---

# TanStack Start Overview

## Quick Reference

### Core Technologies

- React 19 with automatic batching
- TypeScript with strict mode
- TanStack Router for type-safe routing
- TanStack Query for data management
- TanStack Form for form handling
- Zustand for state management
- Tailwind CSS & Shadcn UI for styling

### Project Structure

```sh
app/
├── components/           # Shared UI components
│   ├── ui/              # Shadcn UI components
│   └── features/        # Feature-specific components
├── lib/                 # Core utilities
│   ├── utils/          # Helper functions
│   ├── hooks/          # Custom hooks
│   └── types/          # Shared types
├── features/           # Feature modules
│   └── [feature]/      # Feature-specific code
└── routes/             # Route components
```

## Development Workflow

1. **Setup**

   - Clone repository
   - Install dependencies with `pnpm install`
   - Copy `.env.example` to `.env`
   - Start development with `pnpm dev`

2. **Feature Development**

   - Create feature branch
   - Follow TDD approach
   - Implement patterns
   - Submit PR

3. **Quality Assurance**

   - Run tests
   - Check performance
   - Ensure accessibility
   - Review security

4. **Deployment**
   - Merge to main
   - Automated CI/CD
   - Monitor release
   - Track metrics

## Pattern Usage

### Component Pattern

```typescript
// Example of a well-structured component
export function UserProfile({ id }: { id: string }) {
  // 1. Data hooks
  const user = useUser(id);

  // 2. Derived state
  const isAdmin = user.data?.role === 'admin';

  // 3. Event handlers
  const handleUpdate = useCallback(() => {
    // Update logic
  }, []);

  // 4. Render
  return (
    <div>
      {/* Component JSX */}
    </div>
  );
}
```

### Data Pattern

```typescript
// Example of a data query
export function useUser(id: string) {
  return useQuery({
    queryKey: ['user', id],
    queryFn: () => fetchUser(id),
  });
}
```

## Best Practices

1. **Code Quality**

   - Follow TypeScript strict mode
   - Use proper error handling
   - Implement proper testing
   - Document public APIs

2. **Performance**

   - Optimize bundle size
   - Implement code splitting
   - Use proper caching
   - Monitor metrics

3. **Security**

   - Validate all inputs
   - Implement proper auth
   - Follow security headers
   - Protect sensitive data

4. **Accessibility**
   - Use semantic HTML
   - Follow ARIA patterns
   - Support keyboard
   - Test with readers

## Getting Help

1. **Documentation**

   - Pattern catalog
   - API references
   - Examples
   - Guides

2. **Support**
   - GitHub issues
   - Discord community
   - Stack Overflow
   - Team chat

## Next Steps

1. Review [Architecture](../core/architecture/system.mdc)
2. Check [Patterns](../features/components/patterns.mdc)
3. Study [Examples](../features/components/examples.mdc)
4. Follow [Workflow](../core/workflow/development.mdc)
