---
description: 'Core project concepts, structure, and development workflow'
globs: '*'
---

# Core Project Concepts

## Project Overview

This project follows a modern React stack with TypeScript, focusing on maintainability, type safety, and developer experience.

## Project Structure

```sh
app/
├── components/           # Shared UI components
│   ├── ui/              # Shadcn UI components
│   └── features/        # Feature-specific components
├── lib/                 # Core utilities
│   ├── utils/          # Helper functions
│   ├── hooks/          # Custom hooks
│   └── types/          # Shared types
├── features/           # Feature modules
│   └── [feature]/
│       ├── api/        # Server functions & queries
│       │   ├── queries.ts    # TanStack Query hooks
│       │   └── mutations.ts  # TanStack Mutations
│       ├── components/ # Feature components
│       │   ├── index.ts      # Component exports
│       │   └── [Component]/  # Component files
│       ├── hooks/     # Feature hooks
│       │   └── index.ts      # Hook exports
│       ├── utils/     # Feature utilities
│       └── types/     # Feature-specific types
└── routes/             # Route components
    └── [feature]/      # Feature routes
```

## Technology Stack

### Core Technologies

- React 19 with automatic batching
- TypeScript with strict mode
- TanStack Router for type-safe routing
- TanStack Query for data fetching
- TanStack Form for form handling
- TanStack Start for server functions
- Drizzle ORM for database operations

### UI Layer

- Shadcn UI for components
- Tailwind CSS v4 for styling
- Radix UI for primitives

### Data Layer

- PostgreSQL for database
- Drizzle ORM for queries
- Zod for validation

## Base Patterns

### Component Pattern

```typescript
type BaseProps = {
  className?: string;
  children?: React.ReactNode;
};

// Base component template
export function Component({
  className,
  children,
  ...props
}: BaseProps) {
  return (
    <div className={cn("base-styles", className)} {...props}>
      {children}
    </div>
  );
}
```

### Data Pattern

```typescript
// Base entity type
type BaseEntity = {
  id: string;
  createdAt: Date;
  updatedAt: Date;
};

// Base response type
type ApiResponse<T> = {
  data?: T;
  error?: {
    code: string;
    message: string;
    fields?: Record<string, string>;
  };
};

// Base query options
type BaseQueryOptions = {
  select?: string[];
  include?: Record<string, boolean>;
  where?: Record<string, unknown>;
};
```

## Naming Conventions

### Files and Directories

- Components: `PascalCase.tsx`
- Utilities: `camelCase.ts`
- Server Functions: `$camelCase.ts`
- Hooks: `use[Name].ts`
- Routes: `$paramName.tsx` or `route.tsx`
- Directories: `kebab-case`

### Code Style

- Use 2 space indentation
- Use spaces over tabs
- Use single quotes for strings
- Use semicolons
- No unused variables
- Add space after keywords
- Always use === instead of ==
- Space infix operators
- Space after commas
- Keep else on same line
- Use curly braces for multi-line if
- Handle err function parameter
- Use camelCase for variables
- Use PascalCase for components

### Import Organization

```typescript
// 1. React imports
import { useState } from 'react';

// 2. External libraries
import { useQuery } from '@tanstack/react-query';
import { z } from 'zod';

// 3. Internal absolute imports
import { type User } from '~/types';

// 4. Internal relative imports
import { UserCard } from './UserCard';
```

## Development Workflow

1. Planning

   - Break down requirements
   - Design component structure
   - Plan data flow
   - Consider edge cases

2. Implementation

   - Follow type-safe patterns
   - Write clean, maintainable code
   - Handle errors gracefully
   - Add proper documentation

3. Testing

   - Write unit tests
   - Test edge cases
   - Verify accessibility
   - Check performance

4. Review

   - Code review
   - Self-review checklist
   - Performance audit
   - Accessibility audit

5. Deployment
   - Build optimization
   - Environment checks
   - Monitoring setup
   - Error tracking

## Cross-Cutting Concerns

For specific patterns and implementations, see:

- [API Layer](mdc:.cursor/rules/api/index.mdc) for server functions and API patterns
- [Frontend Layer](mdc:.cursor/rules/frontend/index.mdc) for UI components and forms
- [Data Layer](mdc:.cursor/rules/data/index.mdc) for database operations
- [Error Handling](mdc:.cursor/rules/core/errors.mdc) for error patterns
- [State Management](mdc:.cursor/rules/frontend/state.mdc) for state patterns
- [Testing](mdc:.cursor/rules/testing/index.mdc) for testing strategies
- [Security](mdc:.cursor/rules/security/index.mdc) for security practices
- [Performance](mdc:.cursor/rules/performance/index.mdc) for optimization
- [i18n](mdc:.cursor/rules/frontend/i18n.mdc) for internationalization
