---
description: 'Data protection patterns and best practices for secure data handling'
globs: '**/data/**/*.{ts,tsx}'
---

# Data Protection Patterns

## Overview

Our data protection system ensures secure handling of sensitive information:

- Data encryption
- Secure storage
- Access control
- Data sanitization
- Audit logging

## Core Technologies

- AES encryption
- Bcrypt for hashing
- Input sanitization
- Access logging
- Secure storage

## Implementation

### 1. Data Encryption

```typescript
// security/encryption.ts
export class Encryption {
  static async encrypt(data: string): Promise<string> {
    const iv = crypto.randomBytes(16);
    const cipher = crypto.createCipheriv(
      'aes-256-gcm',
      process.env.ENCRYPTION_KEY,
      iv,
    );

    const encrypted = Buffer.concat([
      cipher.update(data, 'utf8'),
      cipher.final(),
    ]);

    const tag = cipher.getAuthTag();
    return JSON.stringify({
      iv: iv.toString('hex'),
      data: encrypted.toString('hex'),
      tag: tag.toString('hex'),
    });
  }

  static async decrypt(encryptedData: string): Promise<string> {
    const { iv, data, tag } = JSON.parse(encryptedData);
    const decipher = crypto.createDecipheriv(
      'aes-256-gcm',
      process.env.ENCRYPTION_KEY,
      Buffer.from(iv, 'hex'),
    );

    decipher.setAuthTag(Buffer.from(tag, 'hex'));
    const decrypted = Buffer.concat([
      decipher.update(Buffer.from(data, 'hex')),
      decipher.final(),
    ]);

    return decrypted.toString('utf8');
  }
}
```

### 2. Password Hashing

```typescript
// security/hashing.ts
export class PasswordService {
  static async hash(password: string): Promise<string> {
    const salt = await bcrypt.genSalt(12);
    return bcrypt.hash(password, salt);
  }

  static async verify(password: string, hash: string): Promise<boolean> {
    return bcrypt.compare(password, hash);
  }
}
```

### 3. Data Sanitization

```typescript
// security/sanitization.ts
export function sanitizeInput(input: unknown): string {
  if (typeof input !== 'string') {
    return '';
  }

  return input
    .replace(/[<>]/g, '') // Remove < and >
    .replace(/javascript:/gi, '') // Remove javascript: protocol
    .trim();
}
```

## Best Practices

### 1. Data Handling

- Encrypt sensitive data
- Use strong encryption
- Secure key management
- Regular key rotation
- Proper data disposal

### 2. Password Security

- Strong password hashing
- Salt generation
- Password validation
- Secure storage
- Regular updates

### 3. Input Protection

- Input validation
- Output encoding
- SQL injection prevention
- XSS prevention
- CSRF protection

### 4. Monitoring

- Access logging
- Audit trails
- Anomaly detection
- Regular audits
- Incident response

## Related Patterns

- [Authentication](./authentication-patterns.mdc) for auth implementation
- [Authorization](./authorization-patterns.mdc) for access control
- [Security Headers](./security-headers.mdc) for HTTP security
- [Error Handling](../core/error-handling.mdc) for security errors
- [API Security](../api/security-patterns.mdc) for API protection
