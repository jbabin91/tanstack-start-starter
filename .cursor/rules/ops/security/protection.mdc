---
description: 'Overview of security patterns and best practices'
globs: '**/security/**/*.{ts,tsx}'
---

# Security Patterns Overview

## Architecture

Our security architecture implements a comprehensive defense-in-depth strategy:

- Authentication and authorization
- Data protection and encryption
- HTTP security headers
- Input validation and sanitization
- Security monitoring and auditing

## Core Technologies

- JWT for authentication
- RBAC for authorization
- AES for encryption
- Helmet.js for headers
- Monitoring tools

## Pattern Categories

### 1. Authentication

See [Authentication Patterns](./authentication-patterns.mdc) for:

- Token-based authentication
- Session management
- Password security
- Multi-factor authentication
- Social authentication

### 2. Authorization

See [Authorization Patterns](./authorization-patterns.mdc) for:

- Role-based access control
- Permission management
- Resource-level access
- Policy enforcement
- Audit logging

### 3. Data Protection

See [Data Protection Patterns](./data-protection.mdc) for:

- Data encryption
- Secure storage
- Access control
- Data sanitization
- Audit logging

### 4. Security Headers

See [Security Headers Patterns](./security-headers.mdc) for:

- Content Security Policy
- XSS Protection
- CSRF Protection
- Frame Options
- Referrer Policy

## Best Practices

### 1. Security Design

- Defense in depth
- Least privilege
- Secure defaults
- Fail securely
- Complete mediation

### 2. Implementation

- Input validation
- Output encoding
- Error handling
- Logging and monitoring
- Regular updates

### 3. Operations

- Security monitoring
- Incident response
- Regular audits
- Penetration testing
- Security training

### 4. Compliance

- Data protection
- Privacy regulations
- Security standards
- Regular assessments
- Documentation

## Decision Trees

### 1. Authentication Method

```mermaid
graph TD
    A[Start] --> B{Public Access?}
    B -->|Yes| C[No Auth Required]
    B -->|No| D{User Account?}
    D -->|Yes| E{Security Level?}
    D -->|No| F[Social Auth]
    E -->|High| G[MFA Required]
    E -->|Medium| H[Password + Token]
    E -->|Low| I[Token Only]
```

### 2. Authorization Level

```mermaid
graph TD
    A[Start] --> B{Resource Type?}
    B -->|Public| C[Allow All]
    B -->|Private| D{User Role?}
    B -->|Protected| E{Owner?}
    D -->|Admin| F[Full Access]
    D -->|User| G[Limited Access]
    D -->|Guest| H[Read Only]
    E -->|Yes| I[Owner Access]
    E -->|No| J[Deny Access]
```

## Implementation Checklist

### 1. Authentication

- [ ] Implement secure password hashing
- [ ] Set up JWT token management
- [ ] Configure session handling
- [ ] Add MFA support
- [ ] Implement social auth

### 2. Authorization

- [ ] Set up RBAC system
- [ ] Define permission schema
- [ ] Implement access control
- [ ] Add audit logging
- [ ] Configure policies

### 3. Data Protection

- [ ] Set up encryption
- [ ] Configure secure storage
- [ ] Implement sanitization
- [ ] Add access logging
- [ ] Set up monitoring

### 4. Security Headers

- [ ] Configure CSP
- [ ] Set up CORS
- [ ] Add CSRF protection
- [ ] Configure headers
- [ ] Enable monitoring

## Related Patterns

- [API Security](../api/security-patterns.mdc) for API protection
- [Error Handling](../core/error-handling.mdc) for security errors
- [Monitoring](../quality/monitoring.mdc) for security monitoring
- [Testing](../testing/security-testing.mdc) for security testing
- [Deployment](../deployment/security-deployment.mdc) for secure deployment

---

description: 'Security headers patterns and best practices for HTTP security'
globs: '**/security/**/\*.{ts,tsx}'

---

# Security Headers Patterns

## Overview

Our security headers implementation provides comprehensive HTTP security:

- Content Security Policy
- XSS Protection
- CSRF Protection
- Frame Options
- Referrer Policy

## Core Technologies

- Helmet.js
- CSP Builder
- CORS configuration
- Custom middleware
- Security monitoring

## Implementation

### 1. Security Middleware

```typescript
// security/middleware.ts
export const securityMiddleware = [
  // Basic security headers
  helmet(),

  // Content Security Policy
  helmet.contentSecurityPolicy({
    directives: {
      defaultSrc: ["'self'"],
      scriptSrc: ["'self'", "'unsafe-inline'"],
      styleSrc: ["'self'", "'unsafe-inline'"],
      imgSrc: ["'self'", 'data:', 'https:'],
      connectSrc: ["'self'", 'https://api.example.com'],
      fontSrc: ["'self'", 'https://fonts.gstatic.com'],
      objectSrc: ["'none'"],
      mediaSrc: ["'self'"],
      frameSrc: ["'none'"],
    },
  }),

  // CORS configuration
  cors({
    origin: process.env.ALLOWED_ORIGINS?.split(','),
    methods: ['GET', 'POST', 'PUT', 'DELETE', 'OPTIONS'],
    allowedHeaders: ['Content-Type', 'Authorization'],
    credentials: true,
  }),
];
```

### 2. CSRF Protection

```typescript
// security/csrf.ts
export const csrfProtection = [
  // Generate CSRF token
  csrf({
    cookie: {
      httpOnly: true,
      secure: process.env.NODE_ENV === 'production',
      sameSite: 'strict',
    },
  }),

  // Validate CSRF token
  (req: Request, res: Response, next: NextFunction) => {
    if (req.method === 'GET') {
      return next();
    }

    if (!req.csrfToken()) {
      return next(new SecurityError('Invalid CSRF token'));
    }

    next();
  },
];
```

### 3. Security Headers Configuration

```typescript
// security/headers.ts
export const securityHeaders = {
  'Strict-Transport-Security': 'max-age=31536000; includeSubDomains',
  'X-Frame-Options': 'DENY',
  'X-Content-Type-Options': 'nosniff',
  'X-XSS-Protection': '1; mode=block',
  'Referrer-Policy': 'strict-origin-when-cross-origin',
  'Permissions-Policy': 'camera=(), microphone=(), geolocation=()',
};
```

## Best Practices

### 1. Header Configuration

- Use secure defaults
- Regular updates
- Environment-specific settings
- Proper testing
- Monitoring

### 2. CSP Implementation

- Strict policies
- Nonce generation
- Report-only mode
- Violation reporting
- Regular updates

### 3. CORS Security

- Specific origins
- Required methods
- Allowed headers
- Credentials handling
- Preflight requests

### 4. Monitoring

- Header validation
- Policy violations
- Attack detection
- Regular audits
- Incident response

## Related Patterns

- [Authentication](./authentication-patterns.mdc) for auth implementation
- [Authorization](./authorization-patterns.mdc) for access control
- [Data Protection](./data-protection.mdc) for data security
- [Error Handling](../core/error-handling.mdc) for security errors
- [API Security](../api/security-patterns.mdc) for API protection
